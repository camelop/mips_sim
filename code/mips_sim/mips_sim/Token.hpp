#ifndef littleround_MIPS_token
#define littleround_MIPS_token

class Token {
	//to construct the map's latter element
	//give the information pakage of the token
	//also get the key to the function
};

#endif